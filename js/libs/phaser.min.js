// ===== CORE PHASER (Minified) =====
!function(e,t){"object"==typeof exports&&"undefined"!=typeof module?t(exports):"function"==typeof define&&define.amd?define(["exports"],t):t((e=e||self).Phaser={})}(this,function(e){"use strict";/* ... [code minified lengkap] ... */
// ===== EKSTENSI MMO =====
// 1. Hybrid Character Factory
Phaser.GameObjects.GameObjectFactory.register('koCharacter',function(x,y,k,f){
  const c=new this.scene.sys.koCharacter(this.scene,x,y,k,f);
  this.displayList.add(c);return c;
});
Phaser.GameObjects.GameObjectFactory.register('roCharacter',function(x,y,k,f){
  const c=new this.scene.sys.roCharacter(this.scene,x,y,k,f);
  this.displayList.add(c);return c;
});

// 2. MMO Input Plugin
class MMOInputPlugin extends Phaser.Plugins.BasePlugin {
  init(){
    this.keys=this.game.input.keyboard.addKeys('W,S,A,D,1,2,3,4');
    this.game.input.on('pointerdown',p=>{
      if(p.rightButtonDown()) this.game.events.emit('roInteract',p.worldX,p.worldY);
    });
  }
}
Phaser.Plugins.PluginManager.register('MMOInput',MMOInputPlugin);
});